int main(void){

    /*The compiler I use on Windows uses signed chars by default. Therefore,
    the existing code handles EOF correctly since the char array can hold negative
    integers. I'm not sure how to implement a platform independent version but will
    do so in the future.*/

    return 0;
}